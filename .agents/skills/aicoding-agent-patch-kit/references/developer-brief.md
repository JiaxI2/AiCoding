# Developer-only brief

Agents can run this instead of reading the whole kit:

```powershell
apatch brief --format md
```

The brief exposes the minimal command sequence, safety rules, and current effective enable/disable state.
