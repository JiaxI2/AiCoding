# AiCoding Asset Boundary

An asset is a repository-managed functional unit that can be discovered, packaged, installed, updated, verified, exported, or invoked through a stable interface.

Examples include kits, skills, MCP servers, demo code, templates, rulesets, profiles, hooks, validators, generators, runtimes, and agent capabilities.

Individual source files remain implementation details unless they form an independently managed unit.
