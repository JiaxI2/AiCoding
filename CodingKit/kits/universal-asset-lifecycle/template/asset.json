{
  "$schema": "../../../../config/schemas/asset.schema.json",
  "schemaVersion": 1,
  "id": "replace-me",
  "type": "skill",
  "version": "0.1.0",
  "name": "Replace Me",
  "description": "Universal AiCoding asset template.",
  "mode": "managed",
  "payload": "payload",
  "platforms": ["windows", "linux", "darwin"],
  "capabilities": ["validate", "pack", "import", "install", "update", "uninstall", "configure"],
  "dependencies": {
    "required": [],
    "optional": []
  },
  "config": {
    "defaults": "defaults/config.json",
    "schema": "schemas/config.schema.json"
  },
  "metadata": {
    "invocation": {
      "user": true,
      "model": true
    }
  }
}
