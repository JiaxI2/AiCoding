# CodingKit Platforms

This directory is the stable root for AiCoding-owned board, MCU, RTOS, SDK, and toolchain support.

Platform assets remain outside the AiCoding plugin. They must be independently discoverable through the CodingKit manifest and keep platform-specific dependencies below the integration layer.
