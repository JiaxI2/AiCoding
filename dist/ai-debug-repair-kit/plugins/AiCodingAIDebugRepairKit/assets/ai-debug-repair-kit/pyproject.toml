[project]
name = "ai-debug-repair-kit"
version = "0.3.2"
description = "Bounded AI repair loop CLI assets for AiCoding AI Debug Kit."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
airepair = "ai_debug_repair.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
