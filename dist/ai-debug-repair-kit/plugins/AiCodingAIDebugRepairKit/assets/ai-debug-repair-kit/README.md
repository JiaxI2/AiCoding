# ai-debug-repair-kit CLI Asset

Install:

```powershell
python -m pip install --user --force-reinstall .
airepair doctor --output json
```

Initialize profiles:

```powershell
airepair init --workspace . --output json
```
