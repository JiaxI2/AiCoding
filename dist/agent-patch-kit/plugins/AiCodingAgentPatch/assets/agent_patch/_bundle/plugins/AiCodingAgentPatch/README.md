# AiCoding Agent Patch Plugin

This plugin bundles the `aicoding-agent-patch-kit` skill and CLI assets for AiCoding Marketplace packaging.

The Python CLI should be installed on the host with `python -m pip install -e <kit-root>` or the installation scripts in `assets/scripts` when packaged.
